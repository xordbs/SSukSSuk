import styled, { keyframes } from 'styled-components';

const Wrapper = styled.div`
    padding-top: 100px;

    .grid-box{
        border-style: solid;

        margin:10px;
    }
`;

export default Wrapper;
