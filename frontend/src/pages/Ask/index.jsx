import React, { useContext, useEffect } from 'react';

import Layout from '../../layout/';

const Ask = () => {
  return (
    <Layout>
        <h2> Ask 페이지 </h2>
    </Layout>);
};

export default Ask;
