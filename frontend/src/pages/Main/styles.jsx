import styled from 'styled-components';

const Wrapper = styled.div`
  padding-top: 100px;
  & .top-area {
    margin-bottom: 150px;
  }
  & .title {
    font-weight: 700;
  }
`;
export default Wrapper;
