import styled, { keyframes } from 'styled-components';

const Wrapper = styled.div`
  .chart-grid{
    height:200px
  }

  .status-card{
    Background:#FFF56E;
    borderRadius: 100px;
  }
`;

export default Wrapper;
